<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <meta http-equiv="X-UA-Compatible" content="ie=edge">
  <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.4.1/css/bootstrap.min.css" integrity="sha384-Vkoo8x4CGsO3+Hhxv8T/Q5PaXtkKtu6ug5TOeNV6gBiFeWPGFN9MuhOf23Q9Ifjh" crossorigin="anonymous">
  <title>URL Shortener</title>
</head>
<body>
  <div class="container">
    <h1>URL Shrinker</h1>
    <form action="/shortenedURLs" method="POST" class="my-4 form-inline">
      <label for="unshortenedURL" class="sr-only">Url</label>
      <input required placeholder="Url" type="url" name="unshortenedURL" id="unshortenedURL" class="form-control col mr-2">
      <button class="btn btn-success" type="submit">Shrink</button>
    </form>
    
    <table class="table table-striped table-responsive">
      <thead>
        <tr>
          <th>Unshortened URL</th>
          <th>Shortened URL</th>
          <th>Clicks</th>
        </tr>
      </thead>
      <tbody>
        <% shortenedUrls.forEach(shortenedUrl => { %>
          <tr></tr>
            <td><a href="<%= shortenedUrl.unshortened %>"><%= shortenedUrl.unshortened %></a></td>
            <td><a href="<%= shortenedUrl.shortened %>"><%= shortenedUrl.shortened %></a></td>
            <td><%= shortenedUrl.clicks %></td>
          </tr>
        <% }) %>
      </tbody>
    </table>
  </div>
</body>
</html>